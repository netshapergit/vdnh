{% extends '_.admin.base' %}

{% block content %}
	<div>

	</div>
	{% for item in items %}
		<div>
			<a href="{$ url.admin $}/{$ request.segments[1] $}/{$ item.page.id $}/edit">
				{$ item.page.id $}. {$ item.page.path $}
			</a>
		</div>
	{% endfor %}
{% endblock %}
